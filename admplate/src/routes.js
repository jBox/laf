import "./dateFormat";
import React from "react";
import Layout from "./layouts/Layout";

const routes = [{
    id: "page-top",
    routes: [{
        path: "/",
        component: Layout
    }]
}];

export default routes;